[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# This ensures Netlify treats your code as a modern ES Module
[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true
